import { Lacquer, Caveat } from 'next/font/google';

export const lacquer = Lacquer({ subsets: ['latin'], weight: '400' });
export const caveat = Caveat({ subsets: ['latin'], weight: ['400', '600', '700'] });